import React from 'react';
import { Box, Typography, Button } from '@mui/material';
import { Link as RouterLink } from 'react-router-dom';

export default function HeroBanner() {
  return (
    <Box sx={{ display: 'none' }}>
      {/* Hero Banner content moved to ImageSlider */}
    </Box>
  );
}
