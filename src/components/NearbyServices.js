import React from 'react';
import { Typography } from '@mui/material';

export default function NearbyServices() {
  return (
    <div>
      <Typography variant="h6">Nearby Services</Typography>
      <Typography variant="body2">Nearby boat services and amenities listed in Nearby.tsx page</Typography>
    </div>
  );
}