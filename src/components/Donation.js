import React from 'react';
import { Typography } from '@mui/material';

export default function Donation() {
  return (
    <div>
      <Typography variant="h6">Donation Component</Typography>
      <Typography variant="body2">Donation form integrated in Donations.tsx page</Typography>
    </div>
  );
}